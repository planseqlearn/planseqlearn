from setuptools import find_packages, setup

print(
    "Installing DisRep4RL. Dependencies should already be installed with the provided conda env."
)

setup(
    name="disrep4rl",
    version="0.1.0",
    packages=find_packages(),
)
